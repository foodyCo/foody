"""Канарейка для проверки failure-пути пайплайна: намеренно красный тест.

Проверяем, что упавший тест блокирует пуш образов и деплой.
Файл удаляется revert-коммитом сразу после проверки.
"""


def test_pipeline_failure_canary():
    assert False, "Намеренный фейл: красный тест обязан заблокировать деплой"
